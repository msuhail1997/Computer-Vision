import numpy as np

def line_from_pts(p1, p2):
    """ 
    Compute a line from two points

    Input:
        p1: np.array of size (3,) representing point one
        p2: np.array of size (3,) representing point two
        
    Returns:
        l: np.array of size (3,) representing a line in homogenous 
        coordinate that go through point one and point two

    """

    ##### STUDENT CODE START #####
    
    l = []

    ##### STUDENT CODE END #####
    
    return l