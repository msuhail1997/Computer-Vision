import numpy as np

def line_intersection(l1, l2):
    """ 
    Compute the intersection of two line

    Input:
        l1: np.array of size (3,) representing a line in homogenous coordinate 
        l2: np.array of size (3,) representing another line in homogenous coordinate 
        
    Returns:
        pt: np.array of size (3,) representing the intersection of two lines in 
        homogenous coordinate. Remeber to normalize it so its last coordinate
        is either 1 or 0.

    """

    ##### STUDENT CODE START #####

    pt = []

    ##### STUDENT CODE END #####
        
    return pt